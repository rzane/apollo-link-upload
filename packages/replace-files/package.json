{
  "name": "replace-files",
  "license": "MIT",
  "version": "0.1.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "files": [
    "src",
    "lib"
  ],
  "scripts": {
    "prebuild": "rm -rf ./lib",
    "build": "tsc",
    "bench": "tsc && node bench"
  },
  "jest": {
    "preset": "ts-jest",
    "setupFilesAfterEnv": [
      "<rootDir>/test/setup.ts"
    ]
  }
}
